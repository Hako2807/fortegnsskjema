import math
import pg
from calculations import Calculate

def main():
    print("\n———————————————————————————————————————————")
    #display = pg.Display(500, 500)
    #calc = Calculate
    #calc.make_function(calc)
    #calc.calculate_fortegn(calc, calc.factorize_second_degree(calc))

    disp = pg.Display(500, 500)
    while True:
        disp.tick()
    

main()